services:
  api:
    build: .
    environment:
      DATABASE_URL: postgresql+psycopg://nhl:nhl@db:5432/nhl
      SEED_CSV_PATH: /app/data/data_dump.csv
    ports: ["8000:8000"]
    depends_on: [db]
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nhl
      POSTGRES_PASSWORD: nhl
      POSTGRES_DB: nhl
    volumes: [nhl-data:/var/lib/postgresql/data]
volumes:
  nhl-data:
